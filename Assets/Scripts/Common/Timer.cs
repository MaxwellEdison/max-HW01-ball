using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public abstract class Timer : MonoBehaviour
{/*
    public enum TimerType
    {
        Second,
        Minute,
        Hour,
        Day,
        Week,
        Month,
        Year
    }
    float timeScale;
    float second;
    float minute;
    float hour;
    float day;
    float week;
    float month;
    float year;
    // Start is called before the first frame update

    public void timer(TimerType type, float duration)
    {
        switch (type)
        {
            case TimerType.Second:
                Debug.Log("Timer Type = " + type);
                timeScale = second;
                break;
            case TimerType.Minute:
                Debug.Log("Timer Type = " + type);
                timeScale = minute;
                break;
            case TimerType.Hour:
                Debug.Log("Timer Type = " + type);
                timeScale = hour;
                break;
            case TimerType.Day:
                Debug.Log("Timer Type = " + type);
                timeScale = day;
                break;
            case TimerType.Week:
                Debug.Log("Timer Type = " + type);
                timeScale = week;
                break;
            case TimerType.Month:
                Debug.Log("Timer Type = " + type);
                timeScale = month;
                break;
            case TimerType.Year:
                Debug.Log("Timer Type = " + type);
                timeScale = year;
                break;
        }
    }

    void GetTimescale(TimerType type)
    {
        switch (type)
        {
            case TimerType.Second:

                break;
            case TimerType.Minute:
                Debug.Log("Timer Type = " + type);
                timeScale = minute;
                break;
            case TimerType.Hour:
                Debug.Log("Timer Type = " + type);
                timeScale = hour;
                break;
            case TimerType.Day:
                Debug.Log("Timer Type = " + type);
                timeScale = day;
                break;
            case TimerType.Week:
                Debug.Log("Timer Type = " + type);
                timeScale = week;
                break;
            case TimerType.Month:
                Debug.Log("Timer Type = " + type);
                timeScale = month;
                break;
            case TimerType.Year:
                Debug.Log("Timer Type = " + type);
                timeScale = year;
                break;
        }
    }






    // Update is called once per frame
    void Update()
    {
        
    }*/
}
